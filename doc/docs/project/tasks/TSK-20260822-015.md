# Goal
In-Process Embedded llama.cpp Native Engine with Cross-Platform CPU/GPU Architectures for JVM and GraalVM Native Profiles

# Details

## Context
Omniwrench requires a self-contained, in-process LLM inference engine embedding `llama.cpp` and `ggml` shared libraries without relying on external system CLI binaries. The implementation must support cross-architecture execution (x86_64 AVX2/AVX512, ARM64 Neon, Apple Metal, Vulkan/CUDA GPU) and work identically across both standard JVM and GraalVM Native Image profiles.

## Architecture & Class Model

```plantuml
@startuml
skinparam backgroundColor #2e303f
skinparam defaultFontColor #ffffff
skinparam packageBackgroundColor #3b3e54
skinparam classBackgroundColor #1e202f
skinparam classBorderColor #00f0ff
skinparam classFontColor #ffffff
skinparam interfaceBackgroundColor #2d1e3e
skinparam interfaceBorderColor #bd93f9
skinparam interfaceFontColor #ffffff
skinparam arrowColor #00f0ff

package "com.omniwrench.ai" {
    interface "BackendAdapter<T extends MediaType>" as BackendAdapter {
        +getBackendId(): String
        +supports(MediaType, ExecutionMode): boolean
        +execute(ModelRequest<T>): ModelResponse<T>
        +executeStream(ModelRequest<T>): Flux<String>
    }

    interface "StreamingBackendAdapter<T extends MediaType>" as StreamingBackendAdapter {
        +executeStream(ModelRequest<T>): Flux<String>
    }
    BackendAdapter <|-- StreamingBackendAdapter
}

package "com.omniwrench.ai.llamacpp" {
    class "LlamaCppBackendAdapter" as LlamaCppBackendAdapter {
        -bridge: LlamaCppNativeBridge
        -config: LlamaCppConfig
        +execute(ModelRequest): ModelResponse
        +executeStream(ModelRequest): Flux<String>
    }
    StreamingBackendAdapter <|.. LlamaCppBackendAdapter

    class "LlamaCppNativeBridge" as LlamaCppNativeBridge {
        -nativeLoader: NativeLibraryLoader
        -modelHandle: LlamaModelHandle
        -contextHandle: LlamaContextHandle
        -confinedArena: Arena
        +loadModel(Path, LlamaCppConfig): LlamaModelHandle
        +createContext(LlamaModelHandle, LlamaCppConfig): LlamaContextHandle
        +tokenize(LlamaModelHandle, String): int[]
        +decode(LlamaContextHandle, int[]): void
        +sampleNext(LlamaContextHandle): int
        +tokenToPiece(LlamaModelHandle, int): String
    }
    LlamaCppBackendAdapter --> LlamaCppNativeBridge

    class "NativeLibraryLoader" as NativeLibraryLoader {
        +detectOperatingSystem(): OperatingSystem
        +detectArchitecture(): Architecture
        +detectAvailableGpu(): GpuBackend
        +loadLibrary(String): SymbolLookup
        +extractEmbeddedLibrary(String): Path
    }
    LlamaCppNativeBridge --> NativeLibraryLoader

    class "LlamaCppSignalGuard" as LlamaCppSignalGuard {
        +runGuarded(Callable<T>): T
        +installSignalHandlers(): void
    }
    LlamaCppNativeBridge --> LlamaCppSignalGuard

    class "LlamaCppConfig" as LlamaCppConfig <<record>> {
        +modelPath: String
        +contextSize: int
        +gpuLayers: int
        +gpuBackend: GpuBackend
        +threads: int
        +temperature: double
        +topP: double
        +repeatPenalty: double
    }
    LlamaCppBackendAdapter --> LlamaCppConfig
}
@enduml
```

## Native Execution & Token Streaming Flow

```plantuml
@startuml
skinparam backgroundColor #2e303f
skinparam defaultFontColor #ffffff
skinparam sequenceLifeLineBorderColor #00f0ff
skinparam sequenceLifeLineBackgroundColor #1e202f
skinparam sequenceGroupBackgroundColor #3b3e54
skinparam sequenceGroupBorderColor #bd93f9
skinparam sequenceArrowColor #00f0ff
skinparam participantBackgroundColor #1e202f
skinparam participantBorderColor #00f0ff
skinparam participantFontColor #ffffff

autonumber
actor "Agent Engine / TUI" as Client
participant "LlamaCppBackendAdapter" as Adapter
participant "LlamaCppSignalGuard" as Guard
participant "LlamaCppNativeBridge (FFM)" as Bridge
participant "libllama.so / libggml.so" as CppEngine
participant "Reactor FluxSink" as Sink

Client -> Adapter: executeStream(ModelRequest<ChatReasoning>)
activate Adapter

Adapter -> Bridge: loadModel & createContext(config)
activate Bridge
Bridge -> CppEngine: llama_model_load_from_file(path, params)
Bridge -> CppEngine: llama_init_from_model(model, ctx_params)
Bridge --> Adapter: Model & Context Loaded
deactivate Bridge

Adapter -> Bridge: tokenize(systemPrompt + prompt)
activate Bridge
Bridge -> CppEngine: llama_tokenize(prompt, tokens_buf)
Bridge --> Adapter: int[] tokens
deactivate Bridge

Adapter -> Sink: create Flux<String>
Adapter --> Client: Flux<String> stream

loop Until EOS (End-of-Sequence) or Max Tokens
    Adapter -> Guard: runGuarded(() -> bridge.decodeAndSample(tokens))
    activate Guard
    Guard -> Bridge: llama_decode(ctx, batch)
    Bridge -> CppEngine: [C Native Computation on CPU/GPU]
    CppEngine --> Bridge: Decoded Logits
    Bridge -> CppEngine: llama_sampler_sample(smpl, ctx, -1)
    CppEngine --> Bridge: token_id
    Bridge -> CppEngine: llama_token_to_piece(model, token_id)
    CppEngine --> Bridge: String tokenPiece
    Guard --> Adapter: tokenPiece
    deactivate Guard

    Adapter -> Sink: next(tokenPiece)
    Sink -> Client: Stream token to TUI / Web
end

Adapter -> Sink: complete()
deactivate Adapter
@enduml
```

## Third-Party Binary Resolution Strategy

```plantuml
@startuml
skinparam backgroundColor #2e303f
skinparam defaultFontColor #ffffff
skinparam activityBackgroundColor #1e202f
skinparam activityBorderColor #00f0ff
skinparam activityFontColor #ffffff
skinparam diamondBackgroundColor #3b3e54
skinparam diamondBorderColor #bd93f9
skinparam diamondFontColor #ffffff
skinparam arrowColor #00f0ff

start
:Initialize NativeLibraryLoader;

if (Environment variable OMNIWRENCH_LLAMA_LIB_PATH set?) then (yes)
    :Load external user-provided shared library;
    :Verify ABI compatibility;
    stop
else (no)
    if (Embedded resource in JAR/Binary matches Host OS & Arch?) then (yes)
        :Extract to atomic cache (~/.omniwrench/native-cache/v{version}/);
        :Verify SHA-256 Checksum;
        :Load via FFM SymbolLookup.libraryLookup();
        stop
    else (no)
        if (System library /lib64/libllama.so or Homebrew exists?) then (yes)
            :Load from standard OS library paths;
            stop
        else (no)
            :Attempt on-demand compilation via ./helpers/build-native-llamacpp.sh;
            if (CMake & Compiler available?) then (yes)
                :Compile optimized native binary for local CPU/GPU;
                :Cache compiled shared object in local models directory;
                stop
            else (no)
                :Fallback to graceful HTTP / Remote Host / Subprocess mode;
                stop
            endif
        endif
    endif
endif
@enduml
```

## Low-Level C++ Crash & Signal Containment

```plantuml
@startuml
skinparam backgroundColor #2e303f
skinparam defaultFontColor #ffffff
skinparam activityBackgroundColor #1e202f
skinparam activityBorderColor #00f0ff
skinparam activityFontColor #ffffff
skinparam diamondBackgroundColor #3b3e54
skinparam diamondBorderColor #bd93f9
skinparam diamondFontColor #ffffff
skinparam arrowColor #00f0ff

start
:Wrap native FFM call in LlamaCppSignalGuard.runGuarded();
:Register thread-local sigaction for SIGSEGV, SIGBUS, SIGFPE, SIGILL;
:Set recovery checkpoint with sigsetjmp();

if (Native call completed normally?) then (yes)
    :Restore standard signal handlers;
    :Return native result MemorySegment / Token;
    stop
else (Signal Intercepted!)
    :Signal handler executes;
    :Capture stack trace and fault address (siginfo_t.si_addr);
    :siglongjmp() back to recovery checkpoint;
    :Restore signal handlers;
    :Throw structured com.omniwrench.ai.BackendException;
    :Agent Engine catches exception, cleans context, and falls back to safe mode;
    stop
endif
@enduml
```

## Universal Multi-Provider AI SPI

```plantuml
@startuml
skinparam backgroundColor #2e303f
skinparam defaultFontColor #ffffff
skinparam packageBackgroundColor #3b3e54
skinparam classBackgroundColor #1e202f
skinparam classBorderColor #00f0ff
skinparam classFontColor #ffffff
skinparam interfaceBackgroundColor #2d1e3e
skinparam interfaceBorderColor #bd93f9
skinparam interfaceFontColor #ffffff
skinparam arrowColor #00f0ff

interface "BackendAdapter<T extends MediaType>" as BackendAdapter {
    +getBackendId(): String
    +supports(MediaType, ExecutionMode): boolean
    +execute(ModelRequest<T>): ModelResponse<T>
}

interface "StreamingBackendAdapter<T extends MediaType>" as StreamingAdapter {
    +executeStream(ModelRequest<T>): Flux<String>
}
BackendAdapter <|-- StreamingAdapter

class "LlamaCppBackendAdapter" as LlamaCpp {
    <<embedded native>>
    +getBackendId() = "llamacpp"
}
StreamingAdapter <|.. LlamaCpp

class "OpenAiCompatibleBackendAdapter" as OpenAI {
    <<remote HTTP/SSE>>
    +getBackendId() = "openai"
}
StreamingAdapter <|.. OpenAI

class "OllamaBackendAdapter" as Ollama {
    <<local/remote daemon>>
    +getBackendId() = "ollama"
}
StreamingAdapter <|.. Ollama

class "TorchTensorFlowBackendAdapter" as Torch {
    <<embedded JNI/FFM>>
    +getBackendId() = "torch"
}
BackendAdapter <|.. Torch

class "BackendAdapterRegistry" as Registry {
    -adapters: Map<String, BackendAdapter<?>>
    +registerAdapter(BackendAdapter<?>)
    +getAdapter(String): Optional<BackendAdapter<?>>
    +findSupported(MediaType, ExecutionMode): List<BackendAdapter<?>>
}
Registry o-- BackendAdapter
@enduml
```

## Multi-Layered Test Pyramid Design

```plantuml
@startuml
skinparam backgroundColor #2e303f
skinparam defaultFontColor #ffffff
skinparam rectangleBackgroundColor #1e202f
skinparam rectangleBorderColor #00f0ff
skinparam rectangleFontColor #ffffff

rectangle "Layer 1: Unit & Layout Tests\n- NativeLibraryLoaderTest (OS/Arch/GPU detection)\n- LlamaCppConfigTest (Record bounds, defaults, validations)\n- MemoryLayoutTest (FFM struct alignment verification)" as L1

rectangle "Layer 2: Signal & Error Containment Tests\n- LlamaCppSignalGuardTest (Corrupted pointers, null dereference protection)\n- BackendExceptionTest (Error formatting & latency recording)" as L2

rectangle "Layer 3: Functional & Tokenization Tests\n- LlamaCppNativeBridgeTest (Token encoding/decoding, vocabulary lookup)\n- ModelRepositoryAndManagerTest (Ollama/HF search, pull, SHA-256, rm)" as L3

rectangle "Layer 4: Real Weight Integration & E2E Tests\n- Small GGUF Model E2E Test (Pull Gemma/Qwen GGUF, prompt, stream tokens, verify output, clean up)" as L4

rectangle "Layer 5: Dual Profile Native Image Verification\n- GraalVM Native Image binary startup & AOT execution benchmark" as L5

L1 -down-> L2
L2 -down-> L3
L3 -down-> L4
L4 -down-> L5
@enduml
```

## Risk, Limitation & Mitigation Matrix

| Risk / Limitation | Severity | Root Cause | Architectural Mitigation in Omniwrench |
| :--- | :--- | :--- | :--- |
| **Native Segfault / Crash** | **Critical** | Corrupt GGUF weights, null pointers, out-of-bounds context. | Implement `LlamaCppSignalGuard` using POSIX signal handling (`sigaction` trapping `SIGSEGV`, `SIGBUS`, `SIGFPE`, `SIGILL`) and `siglongjmp` back into safe Java boundary. |
| **Native Off-Heap Memory Leak** | **High** | Forgetting to free large KV cache or model tensor weights. | Encapsulate all native handles in `AutoCloseable` wrappers tied to scoped Java `Arena.ofConfined()` or deterministic `SessionContext` lifecycles. |
| **GPU Driver Incompatibility** | **Medium** | Missing CUDA toolkit, outdated Vulkan loaders, driver mismatch. | Safe fallback cascade: CUDA -> ROCm -> Vulkan -> CPU AVX2. If GPU initialization fails, automatically restart context on pure CPU SIMD. |
| **Platform ABI Divergence** | **Medium** | Structure padding and pointer sizing differences between OSes. | Use explicit FFM `ValueLayout.JAVA_LONG`, `JAVA_INT`, and `ADDRESS` layouts rather than hardcoded byte offsets. |
| **GraalVM AOT Build Time** | **Low** | Static analysis overhead of native libraries. | Isolate native symbol lookup behind dynamic runtime initializers (`--initialize-at-run-time=com.omniwrench.ai.llamacpp`). |

## Requirements
- [x] [TSK-20260822-015.1] Create `omniwrench-ai` native shared library loader (`NativeLibraryLoader`) detecting OS and architecture (`linux-x86_64`, `linux-aarch64`, `macos-arm64`, `windows-x86_64`) and extracting bundled prebuilts. (Traceability: `REQ-00090`, `REQ-00093`).
- [x] [TSK-20260822-015.2] Implement Java 22+ Foreign Function & Memory (FFM) / JNI in-process bindings (`LlamaCppNativeBridge`) for `llama_backend_init`, `llama_model_load_from_file`, `llama_init_from_model`, `llama_tokenize`, `llama_decode`, and token samplers. (Traceability: `REQ-00090`, `REQ-00093`).
- [x] [TSK-20260822-015.3] Implement POSIX signal guard (`LlamaCppSignalGuard`) and error boundaries to prevent native memory faults from crashing JVM or GraalVM processes. (Traceability: `REQ-00090`, `REQ-00093`).
- [x] [TSK-20260822-015.4] Support GPU offloading parameters (`gpuLayers`, `mainGpu`, `splitMode`) across CUDA, ROCm, Vulkan, and Metal backends. (Traceability: `REQ-00090`, `REQ-00093`).
- [x] [TSK-20260822-015.5] Support token streaming (`StreamingBackendAdapter<MediaType.ChatReasoning>`) emitting real-time tokens to Reactor `Flux<String>`. (Traceability: `REQ-00090`, `REQ-00093`).
- [x] [TSK-20260822-015.6] Ensure compatibility with GraalVM Native Image (`-H:+ForeignAPISupport` or `jni-config.json` registration). (Traceability: `REQ-00025`, `REQ-00090`).
- [x] [TSK-20260822-015.7] Add comprehensive unit and integration tests with zero hardcoded mocks per CS-0055. (Traceability: `REQ-00090`, `REQ-00092`).
- [x] [TSK-20260822-015.8] Update project documentation, architecture diagrams, and user guides. (Traceability: `REQ-00020`).

# Status
- [ ] Not started
- [ ] In progress
- [x] Completed

