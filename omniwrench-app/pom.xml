<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.omniwrench</groupId>
        <artifactId>omniwrench</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>omniwrench-app</artifactId>
    <name>Omniwrench Application</name>
    <description>Spring Boot assembly module: main entry point, plugin loader, and packaged fat JAR.</description>

    <dependencies>
        <!-- All internal modules -->
        <dependency>
            <groupId>com.omniwrench</groupId>
            <artifactId>omniwrench-core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.omniwrench</groupId>
            <artifactId>omniwrench-tools</artifactId>
        </dependency>
        <dependency>
            <groupId>com.omniwrench</groupId>
            <artifactId>omniwrench-ai</artifactId>
        </dependency>
        <dependency>
            <groupId>com.omniwrench</groupId>
            <artifactId>omniwrench-tui</artifactId>
        </dependency>
        <dependency>
            <groupId>com.omniwrench</groupId>
            <artifactId>omniwrench-web</artifactId>
        </dependency>

        <!-- Spring Boot starters -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>info.picocli</groupId>
            <artifactId>picocli-spring-boot-starter</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
